en el primer conflicto decidi dejar los cambios propuestos por Emma en el archivo Clasicos
☠️❌💀
Antes de empezar a trabajar hice un git pull, hubo un cambio remoto pero como no habia hecho un cambio local, no hubo problemas. 
En remoto aparece una nueva rama, al querer hacer modificaciones en la nueva rama, no la encuentro, las veo con git fetch, verifico que la rama este en remoto con git branch -r veo cuantas ramas hay y al ver las dos ramas hago un git checkout -b "probando crear rama" origin/"probando crear rama" para crear esa nueva rama en local.
lo verifico con $ git branch -vv
Hago cambios en "Miguelito.txt" lo agrego al area staging y hago un commit. 
Se genero un conflicto, el archivo "conflictos.txt" que esta en main, al estar en la rama"probando crear rama" no tiene seguimiento no permite cambiar a main.
borro el txt con rm conflictos.txt
hago el commit git commit -m "Eliminado conflictos.txt de probando-crear-rama"
y cambio a main git checkout main
Vuelvo a la rama main y edito ese mismo archivo miguelito lo agrego al staging
git add Miguelito.txt
hago el comit  git commit -m "actualizacion de Miguelito.txt"
no hubo conflicto
estoy situada en main hago un merge 
git merge probando-crear-rama
se genero un conflicto porque modifique la misma parte en amabas ramas.
edito el txt, hago un add y commit
y por ultimo un git push origin main
Por ultimo borro la rama extra git branch -d probando-crear-rama
ya hice el merge entonces fuerzo el borrado
 git branch -D probando-crear-rama
Tambien borro la rama nueva remota git push origin --delete probando-crear-rama
Punto 9 del ejercicio terminado.